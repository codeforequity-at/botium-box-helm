apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: botium-box-storage
  labels:
    name: botium-box-storage
    app: botium-box
    tier: frontend
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: {{ .Values.STORAGE_CLASS }}
  resources:
    requests:
      storage: 5Gi
